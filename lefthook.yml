pre-commit:
  piped: true
  commands:
    govet:
      tags: backend style
      glob: "*.go"
      run: go vet ./...
    
    go-tidy:
      glob: "*.go"
      run: go mod tidy

    go-fmt:
      glob: "*.go"
      run: go fmt ./...
    
    update:
      run: git update-index --again
